#ifndef CHILD_H
#define CHILD_H

#include <string>
using namespace std;

class Child {
    public:
        // Data members
        string name;

        // Constructors
        Child();
        Child(string name);
};
#endif // CHILD_H